name: First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
        run: |
            echo "Hello world"
            echo "Hello world1"
            
      - name: print goodbye
        run: |
          echo "Done world"
          echo "Done world"
      
      
    
